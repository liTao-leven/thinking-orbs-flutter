name: thinking_orbs_example
description: A minimal example app showing the thinking_orbs_flutter package.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  thinking_orbs_flutter:
    path: ../

flutter:
  uses-material-design: true
